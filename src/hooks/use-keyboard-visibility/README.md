## useKeyboardVisibility 封装

> _监听键盘弹起/收起_

### 使用方法

```js
import useKeyboardVisibility from '@/hooks/use-keyboard-visibility'
;<script setup lang="ts">
  const {isKeyboardVisible} = useKeyboardVisibility()
</script>
```

详见 useKeyboardVisibility/index.ts
